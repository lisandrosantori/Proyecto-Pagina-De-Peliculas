@*

    Formulario carga de pelicula
        Carga de datos básicos (Director actores, posterlink etc).
        Carga de Cadena (seleccionado por cuidad)
        Carga de cines de esa cadena.
        Carga de salas por cine.

    Se pide:
        Bosquejo a mano alzada de los formualarios.
        Que cargar primero y que despues
        Sugerir que tener pre cargado por fuera (procesos externos).

*@
<!doctype html>
<html lang="es">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://getbootstrap.com/docs/5.2/assets/css/docs.css" rel="stylesheet">
    <title>ABM de Peliculas</title>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
</head>
<body class="p-3 m-0 border-0 bd-Peliculas">

    <div class="container mt-5 mb-5">
        <form  action="" method="post">
            <div class="row">
                <div class="col-6">

                    <div class="mb-3">
                        <label for="PeliculasFormControlNombre" class="form-label">Nombre pelicula</label>
                        <input type="text" class="form-control" id="PeliculasFormControlNombre" placeholder="titulo">
                    </div>
                    <div class="mb-3">
                        <label for="PeliculasFormControlPaisOrigen" class="form-label">Pais de origen</label>
                        <input type="text" class="form-control" id="PeliculasFormControlPaisOrigen" rows="3" spellcheck="true" data-ms-editor="true"></input>
                    </div>
                    <div class="mb-3">
                        <label for="PeliculasFormControlDirector" class="form-label">Director</label>
                        <input type="text" class="form-control" id="PeliculasFormControlDirector" rows="3" spellcheck="true" data-ms-editor="true"></input>
                    </div>
                    <label for="PeliculasFormControlGenero" class="form-label">Género</label>
                    <select id="PeliculasFormControlGenero" class="form-select form-select-sm" aria-label=".form-select-sm Peliculas">
                        <option selected>Seleccione un genero</option>
                        @{
                            int num_genero = 1; @*Deberia ser el id del genero*@

                            foreach (var g in @ViewBag.generos)
                            {
                                        <option value=num_genero.Tostring()>@g.Nombre</option>
                                num_genero++;
                            }
                        }
                    </select>
                </div>
                <div class="col-6">
                    <div class="mb-3">
                        <label for="PeliculasFormControlTrailer" class="form-label">URL del trailer</label>
                        <input type="text" class="form-control" id="PeliculasFormControlTrailer" placeholder="Link del trailer en YouTube">
                    </div>
                    <div class="mb-3">
                        <label for="formFileSm" class="form-label">Seleccione la imagen del poster</label>
                        <input class="form-control form-control-sm" id="formFileSm" type="file">
                    </div>
                    <div class="mb-3">
                        <label for="PeliculasFormControlResumen" st class="form-label">Resumen</label>
                        <textarea class="form-control resumenCrearPelicula" id="PeliculasFormControlResumen" rows="3" spellcheck="true" data-ms-editor="true"></textarea>
                    </div>
                </div>
            </div>
            <div class="row mt-5">

                <div class="col-4">
                    <select class="form-select" multiple aria-label="multiple select Peliculas">
                        <option selected>Seleccione el elenco</option>
                        @{
                            int num_actores = 1; @*Deberia ser el id del actor*@

                            foreach (var a in @ViewBag.actores)
                            {
                                        <option value=num_actores.Tostring()>@a.Nombre</option>
                                num_actores++;
                            }
                        }
                    </select>
                </div>
                <div class="col-4">
                    <select class="form-select" multiple aria-label="multiple select Peliculas">
                        <option selected>Seleccione los cines</option>
                        <@{
                            int num_cines = 1; @*Deberia ser el id del cine*@
                            foreach (var c in @ViewBag.cines)
                            {
                                                <option value=num_cines.Tostring()>@c.Nombre</option>
                    num_cines++;
                }
            }
                    </select>
                </div>
                <div class="col-4 btnGuardarCrearPelicula">
                    <button class="btn btn-primary" type="submit">Guardar</button>
                </div>
            </div>


        </form>
    </div>
</body>
</html>